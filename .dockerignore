.venv
__pycache__
*.py[oc]
.env
.git
.gitignore
docs/
tests/
metrics/
img/
output/
*.lock
.DS_Store

# Allow pyproject.toml (needed for `pip install .`) and README.md (required by pyproject)
# All other .toml/.md are excluded by being unlisted in build context COPY directives
